<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp"
    tools:context="pl.lukaszbyjos.relationstest.MainActivity">

    <Button
        android:id="@+id/addProjectButton"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="6dp"
        android:text="clean and add project"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_default="wrap"/>


    <Button
        android:id="@+id/editEntity"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="edit task entity"
        android:padding="6dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/addProjectButton"
        app:layout_constraintWidth_default="wrap"/>

    <Button
        android:id="@+id/editProject"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="edit project entity"
        android:padding="6dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/editEntity"
        app:layout_constraintWidth_default="wrap"/>

</android.support.constraint.ConstraintLayout>
